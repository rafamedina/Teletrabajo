<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Rule: Employees can only see their own telework requests -->
        <record id="rule_telework_request_employee" model="ir.rule">
            <field name="name">Telework Request: Employee (Own Only)</field>
            <field name="model_id" ref="model_telework_request"/>
            <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- Rule: Managers can see requests of their subordinates -->
        <record id="rule_telework_request_manager" model="ir.rule">
            <field name="name">Telework Request: Manager (Subordinates)</field>
            <field name="model_id" ref="model_telework_request"/>
            <field name="domain_force">[('manager_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>
